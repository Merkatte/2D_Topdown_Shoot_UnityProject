using UnityEngine;

public enum UpgradeCategory
{
    Player,
    Weapon
}

public enum PlayerStatType
{
    Health,
    MoveSpeed
}

public enum WeaponStatType
{
    Damage,
    FireRate,
    BulletNum
}
